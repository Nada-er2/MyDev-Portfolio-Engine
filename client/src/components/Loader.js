import ClipLoader from "react-spinners/ClipLoader";

function Loader() {
  return (
    <div className="loader-page">
      <ClipLoader size={40} color="#725AC1" />
    </div>
  );
}

export default Loader;